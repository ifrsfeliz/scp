<h1 class="title">Exibindo Professor</h1>

<div class="row">
  <div class="col-md-6">
    <h4 class="title">Dados</h4>

    <dl class="dl-horizontal">
      <dt>Nome:</dt>
      <dd><%= @professor.nome %></dd>
      <dt>Cpf:</dt>
      <dd><%= @professor.cpf %></dd>
      <dt>Rg:</dt>
      <dd><%= @professor.rg %></dd>
      <dt>Email:</dt>
      <dd><%= @professor.email %></dd>
      <dt>Link Lattes:</dt>
      <dd><%= @professor.link_lattes %></dd>
      <dt>Telefone:</dt>
      <dd><%= @professor.telefone %></dd>
      <dt>Celular:</dt>
      <dd><%= @professor.celular %></dd>
    </dl>
  </div>

  <div class="col-md-6">
    <h4 class="title">Documentos</h4>
    
    <table class="table">
      <tr>
        <th>Nome</th>
        <th>Ações</th>
      </tr>
    
      <% if @professor.documents.any? %>
        <% @professor.documents.each do |d| %>
          <tr>
            <td><%= link_to d.file_file_name, d.file.url %></td>
            <td><%= link_to 'Deletar', d, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
          </tr>
        <% end %>
      <% else %>
        <tr>
          <td>Nenhum documento cadastrado até o momento.</td>
        <tr>
      <% end %>
    
    </table>
  </div>
</div>

<hr />

<%= link_to 'Editar', edit_professor_path(@professor), class: 'btn btn-warning' %>
<%= link_to 'Voltar', professors_path, class: 'btn btn-default'%>
