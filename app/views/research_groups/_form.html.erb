<%= simple_form_for(@research_group) do |f| %>
  <%= f.error_notification %>

    <%= f.input :nome %>
    <%= f.association :professor, label_method: :nome, value_method: :id, include_blank: false, input_html: {class: 'chosen-select'}, label: 'Líder' %>
    <%= f.input :ultima_verificacao, as: :date_picker, label: 'Última Verificação' %>

    <%= f.button :submit, class: 'btn btn-primary'%>
<% end %>
